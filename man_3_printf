.\" Manual page for custom printf function
    .SH NAME
printf \- custom printf function

	.SH SYNOPSIS
.B #include <stdio.h>
.PP
.B int printf(const char *format, ...);

	.SH DESCRIPTION
The
.B printf
function writes output to the standard output stream according to a format string and additional arguments.

	.SH RETURN VALUE
The
.B printf
function returns the number of characters printed (excluding the null byte used to end output to strings).

	.SH SPECIFIERS
The
.B printf
function supports the following format specifiers:

.TP
.B \fB%c\fR
    Prints a single character.

.TP
.B \fB%s\fR
    Prints a null-terminated string.

.TP
.B \fB%%\fR
    Prints a percent sign.

.TP
.B \fB%d\fR, \fB%i\fR
    Prints a signed integer.

.TP
.B \fB%u\fR
    Prints an unsigned decimal integer.

.TP
.B \fB%o\fR
    Prints an unsigned octal integer.

.TP
.B \fB%x\fR, \fB%X\fR
    Prints an unsigned hexadecimal integer (\fB%x\fR uses lowercase letters, \fB%X\fR uses uppercase).
